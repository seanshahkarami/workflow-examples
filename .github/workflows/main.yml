on:
  push:

jobs:
  call-workflow:
    name: Build Images
    uses: ./example.yml@main
    with:
      username: robot
  call-workflow-again:
    name: Run Tests
    uses: ./example.yml@main
    with:
      username: bingbong
  call-workflow-final:
    name: Setup Docs
    uses: ./example.yml@main
    with:
      username: wilson
